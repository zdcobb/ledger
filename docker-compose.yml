version: "3.9"
services:
  server:
    build: ./server
    volumes:
      - ./server:/app
    ports:
      - "3000:3000"
  ui:
    build: ./ui
    volumes:
      - ./ui:/app
    ports:
      - "8080:8080"
    environment:
      - NODE_OPTIONS=--openssl-legacy-provider=value
      